import chords from "@/assets/chords";
import { Song } from "@/types";

const { C_OPEN, F, D_SEMI_BARRE, A_OPEN } = chords

const song: Song = {
  timeSignature: {
    beats: 4,
    denominator: 4,
  },
  measures: [
    [],
  ]
}

export default song